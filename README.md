# AI Recommendation System

## Quick Start
```bash
pip install -r requirements.txt
cp .env.example .env        # Add your GROQ_API_KEY
python app.py               # Runs on http://localhost:5001
```

## Pages
- `http://localhost:5001/`            → Live preview (user-facing)
- `http://localhost:5001/admin`       → Admin panel (login required)

## Default Admin Login
- Username: `admin`
- Password: `admin123`

## Get Groq API Key
https://console.groq.com (Free, fast inference)
